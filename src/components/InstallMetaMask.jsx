import styled from 'styled-components'

const InstallContainer = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
`

const InstallText = styled.h1``

export const InstallMetaMask = () => (
  <InstallContainer>
    <InstallText>Install metamask</InstallText>I
  </InstallContainer>
)
